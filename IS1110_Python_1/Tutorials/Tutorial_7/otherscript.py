import myscript                 # brings in the code but doesn’t run the bottom part

print("Now we are in another file.")
myscript.main()                 # we can still call the function if we want
